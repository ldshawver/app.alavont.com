Diagnose, test, configure, and harden the entire printer system until receipt and label printing work correctly end-to-end in MyOrder.fun.

Goal:
The printers are not reliably working yet. I need a full diagnostic + repair pass that continues through configuration, testing, routing validation, bridge validation, and print job execution until both receipt and label printing work correctly.

Known environment facts:
- We are using a Mac print bridge
- Bridge URL is intended to be reached over Tailscale
- Receipt printer queue on the Mac: Reciept_POS80_Printer
- Label printer queue on the Mac: Label_Themal_Printer
- Bridge runs on port 3001 currently unless changed in code/config
- Connection type in app should be bridge
- Receipt jobs and label jobs must route independently by printer role
- Business sitter routing must work
- The app should support test print, real receipt print, and label print
- The bridge currently requires x-api-key on /print
- There have already been issues with missing credentials on apiFetch, printer role resolution, wrong default port, and broken test print DB inserts

Mission:
Do a full printer reliability pass and keep going until all of these work:
1. bridge health check
2. printer list retrieval
3. receipt test print
4. label test print
5. printer routing by role
6. business sitter/operator routing fallback
7. actual print job save/logging
8. admin UI save/load of printer configs

Perform this in phases.

PHASE 1 — DIAGNOSE CURRENT STATE
1. Audit current printer-related code paths end-to-end:
   - printer settings save/load routes
   - print bridge client
   - print job DB insert path
   - test print route
   - receipt print route
   - label print route
   - printer routing resolver
   - operator/business sitter resolver
   - printer health check code
2. Identify all current assumptions and mismatches, especially:
   - wrong bridge port usage (3100 vs 3001)
   - hardcoded printer names
   - wrong role resolution
   - wrong connection type filters
   - missing credentials in authenticated API calls
   - bridge API key not included on /print
   - DB constraints breaking test print
   - app storing wrong printer names or URLs
3. Add temporary structured diagnostic logs to show:
   - selected printer config
   - bridge URL used
   - role requested
   - operator resolved
   - request sent to bridge
   - bridge response
   - DB insert status
   - print job status changes

PHASE 2 — VERIFY AND REPAIR CONFIG MODEL
4. Ensure printer config model supports at minimum:
   - id
   - name
   - role (receipt | label)
   - connectionType
   - bridgeUrl
   - printerNameOnBridge
   - bridgeApiKey (stored securely, not exposed in UI after save)
   - isActive
   - priority
   - tenant/company scope if applicable
5. Validate that existing printer records are normalized correctly:
   - receipt printer should use:
     - name: Reciept_POS80_Printer
     - role: receipt
     - connectionType: bridge
     - printerNameOnBridge: Reciept_POS80_Printer
   - label printer should use:
     - name: Label_Themal_Printer
     - role: label
     - connectionType: bridge
     - printerNameOnBridge: Label_Themal_Printer
6. Make bridge URL and bridge API key configurable per printer or per bridge profile, but ensure actual print requests use the correct saved values.

PHASE 3 — BRIDGE CONNECTIVITY TESTS
7. Add or fix backend bridge connectivity diagnostics:
   - GET bridge /health
   - GET bridge /printers
   - authenticated POST bridge /print
8. Add admin-only actions/buttons:
   - Test Bridge Health
   - Load Bridge Printers
   - Test Receipt Print
   - Test Label Print
9. For diagnostics, show:
   - HTTP status
   - parsed bridge response
   - selected URL
   - selected printer name
   - whether x-api-key was included
10. If bridge URL is unreachable, show a clean error with likely causes:
   - wrong Tailscale IP
   - wrong port
   - bridge not running
   - firewall / connectivity issue

PHASE 4 — FIX TEST PRINT FLOW
11. Fix test print so it no longer fails because of DB constraints like fake order_id = 0.
12. For test prints:
   - do not require a real order
   - allow order_id to be null for job_type = test
   - or bypass order-bound print_jobs insert if needed and use diagnostics log path instead
13. Keep print job logging, but do not let test-print DB design block actual printer testing.
14. Return clean admin UI errors, never raw HTML stack traces.

PHASE 5 — FIX PRINTER ROUTING
15. Update printer resolution so printers are selected by role, not connection type.
16. Routing rules:
   - receipt jobs -> active receipt printer
   - label jobs -> active label printer
17. If business sitter is clocked in and routing rules depend on operator, include:
   - global_admin
   - tenant_admin
   - business_sitter
18. Add diagnostic output showing:
   - resolved operator
   - resolved printer role
   - selected printer config id/name

PHASE 6 — REAL PRINT EXECUTION
19. Ensure backend print execution sends:
   - bridge URL
   - x-api-key
   - printer name
   - text or image payload
   - copies
20. If bridge supports dynamic printer selection in request body, use that.
21. If bridge currently only uses one env-configured queue, upgrade integration so request can specify printer name dynamically.
22. Support:
   - receipt text printing
   - label image printing
23. For label printer:
   - verify generated PNG path exists before sending
   - ensure bridge path handles image jobs if implemented
24. Log success/failure per print attempt.

PHASE 7 — ADMIN UI HARDENING
25. Fix admin printer UI so:
   - save works
   - load works
   - edit works
   - test print works
   - printer status reflects actual health test result
26. If browser requests need auth cookies, ensure fetch/apiFetch includes credentials: "include".
27. Add clear field validation:
   - bridge URL required for bridge connection type
   - printer name on bridge required
   - role required
   - prevent duplicate active printer roles unless intentionally allowed
28. Add “Last tested”, “Last success”, and “Last error” fields in admin UI if practical.

PHASE 8 — PRESET KNOWN-GOOD VALUES
29. Add an admin action or seed helper that can auto-fill the known-good printer config values:
   Receipt:
   - role: receipt
   - connectionType: bridge
   - bridgeUrl: http://100.103.51.6:3001
   - printerNameOnBridge: Reciept_POS80_Printer

   Label:
   - role: label
   - connectionType: bridge
   - bridgeUrl: http://100.103.51.6:3001
   - printerNameOnBridge: Label_Themal_Printer

30. Make these editable in case the Tailscale IP changes.

PHASE 9 — QA / ACCEPTANCE TESTS
31. Verify all of these scenarios:
   - bridge health loads successfully
   - bridge printer list loads successfully
   - receipt printer test job prints
   - label printer test job prints
   - printer settings save and reload
   - role-based routing chooses correct printer
   - business sitter can be resolved in routing flow
   - test print no longer fails on DB insert
   - actual order receipt print routes to receipt printer
   - actual thank-you/image label print routes to label printer
32. Add automated tests where practical for:
   - printer config validation
   - routing by role
   - bridge client request building
   - test print DB logic
   - authenticated fetch behavior

PHASE 10 — OUTPUT / DELIVERABLES
33. Deliver:
   - root cause summary of every printer issue found
   - list of fixes applied
   - exact printer config values currently expected
   - exact bridge request shape used for receipt and label jobs
   - any remaining manual steps required on the Mac bridge side
34. Do not stop at code changes alone. Confirm the workflow is testable and working.
35. Keep changes production-safe, secure, and least-privilege.

Important guardrails:
- Do not expose bridge API keys in the UI after save
- Do not leak raw stack traces to admin UI
- Do not trust frontend printer fields without server validation
- Do not hardcode wrong printer queues or ports
- Do not assume connection type implies printer role

Expected result:
By the end of this pass, receipt and label printers should be correctly configured, testable from the admin UI, routed by role, and able to print reliably through the Mac bridge over Tailscale.