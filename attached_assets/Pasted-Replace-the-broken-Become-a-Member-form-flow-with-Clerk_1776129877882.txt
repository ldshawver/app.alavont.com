Replace the broken “Become a Member” form flow with Clerk Waitlist mode. Branding is nice, but Clerk’s waitlist flow must be the source of truth and must supersede the custom form if there is any conflict.

Goal:
- “Become a Member” should route to the Clerk waitlist flow
- remove or disable the current broken custom form submission path
- keep branded styling if possible, but do not break Clerk’s required setup
- use Clerk’s official waitlist behavior correctly

Requirements from Clerk:
- Waitlist mode must be enabled in the Clerk Dashboard
- the <Waitlist /> component should be used for the waitlist page
- if using <Waitlist />, the app must provide waitlistUrl through ClerkProvider or relevant Clerk component configuration
- sign-in/sign-up behavior must respect Clerk waitlist mode

Implementation steps:

1. Clerk Dashboard setup
- In Clerk Dashboard, enable Waitlist mode for the application
- Confirm the production instance is the one being configured
- Do not continue assuming the frontend form alone is enough

2. Remove broken custom form flow
- Find the current “Become a Member” form and disable/remove the submission logic that currently fails
- Keep any styling, copy, and layout that is useful, but the actual submission must no longer use the broken custom form path
- If needed, keep the current branded section as a landing/marketing section only, with the CTA routing to the waitlist page

3. Create a dedicated waitlist route/page
- Add a dedicated waitlist page/route, e.g.:
  - /waitlist
- Render Clerk’s official waitlist UI on that page using the <Waitlist /> component
- Keep page styling aligned with the brand where possible, but do not interfere with Clerk functionality

4. Wire ClerkProvider correctly
- Ensure the app is wrapped in ClerkProvider at the correct app entry point
- Provide waitlistUrl in ClerkProvider so Clerk knows the canonical waitlist route
- Also verify the correct Clerk production env vars are used for the current framework

5. Route “Become a Member” correctly
- Change all “Become a Member” buttons/links/CTAs so they navigate to /waitlist
- Do not post to the old form endpoint anymore
- Do not keep dead submit handlers attached

6. Respect framework-specific Clerk setup
- Detect the actual frontend framework and use the correct Clerk SDK/config approach
- If the frontend is Vite, keep the correct VITE_ Clerk env usage
- If Next.js is used anywhere for frontend routing, follow the proper ClerkProvider placement for that framework
- Do not assume the old broken setup was correct

7. Branded fallback behavior
- If a branded custom pre-waitlist section is kept, it should:
  - show explanation/copy
  - have a single CTA button such as “Join the Waitlist”
  - route to the official Clerk waitlist page
- Do not try to duplicate Clerk’s waitlist submission logic in a custom form unless it is explicitly supported and fully working

8. Verify sign-in/sign-up behavior under waitlist mode
- Confirm the app behavior matches Clerk’s waitlist mode:
  - SignIn only works for approved users
  - SignUp is invitation-restricted if waitlist mode is active
  - users without approval are directed appropriately
- Make sure user messaging is clear and branded, but still Clerk-compliant

9. Admin-safe cleanup
- Remove dead form code, unused hooks, broken API calls, and stale validation logic related to the custom “Become a Member” form
- Keep the codebase clean and obvious

10. Deliverables
Provide:
- the new waitlist route/page
- updated ClerkProvider config with waitlistUrl
- updated “Become a Member” CTA routing
- removal/disablement of broken custom form handling
- concise explanation of what changed
- any required Clerk Dashboard actions still needed manually

11. Manual verification checklist
After implementation, verify:
- “Become a Member” navigates to /waitlist
- Clerk waitlist form renders correctly
- form submission succeeds
- no old broken form request fires
- branding still looks coherent
- app respects Clerk waitlist restrictions